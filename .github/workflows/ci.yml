name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  bun:
    uses: Glyndor/.github/.github/workflows/bun-ci.yml@f72a52755c737ad4ae0a61e56ac43e2edc7280c5 # v1.1.0
    with:
      coverage-threshold: 90
